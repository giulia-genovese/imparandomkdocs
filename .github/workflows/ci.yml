name: Deploy MkDocs site to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          pip install --upgrade pip  # Ensure the latest pip
          pip install mkdocs mkdocs-material mkdocs-glightbox

      - name: Verify MkDocs Installation
        run: |
          mkdocs --version  # Ensure MkDocs is installed correctly

      - name: Build MkDocs site
        run: |
          mkdocs build --verbose  # Run mkdocs build with verbose output for debugging
          
      - name: Debug site directory
        run: ls -l ./site  # Ensure the site directory is populated

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: github-pages
          path: ./site

  deploy:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: build

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: github-pages

      - name: Debug downloaded artifact
        run: ls -l ./site  # Ensure the downloaded artifact is correctly extracted

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2



